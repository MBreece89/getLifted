#!/usr/bin/env bash
# Pre-commit: fail if tool adapter files (GEMINI.md, .cursor/) have drifted from AGENTS.md.
# Install: bash scripts/install-hooks.sh
set -euo pipefail
REPO_ROOT="$(git rev-parse --show-toplevel)"
bash "$REPO_ROOT/scripts/sync-agent-config.sh" --check
